//! `nexq-server` binary. The only crate that knows about all the others: reads config,
//! opens the configured stores, and mounts whichever facades are enabled.
//!
//! Backends can be compiled out for slim builds via this crate's features.

fn main() {
    todo!("nexq server")
}
