[package]
name = "nexq-store-conformance"
description = "Shared conformance test suite that every NexQ storage backend runs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
nexq-core = { workspace = true }
# `rt` for the concurrency cases, `time` for the ones that wait out a claim.
tokio = { workspace = true, features = ["rt", "time"] }

[lints]
workspace = true
